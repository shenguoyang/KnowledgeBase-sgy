**在线检测转离线检测调试问题：**

1.leave后解除client和sessionId的绑定，导致异步保存示教参数发布出去

2.离线示教完成没有解除绑定关系

3.离线示教与在线检测可同时存在，需要两套独立的session，在离线示教失败和成功的回调中sessionId没有赋值对

4.修改检测参数时会修改颜色配置，而在颜色配置中有在线检测条件的限制，没有把它取消掉

5.没有将离线示教的保存参数串起来

6.检测服务端在申请session时，会有是否在线检测状态的限制

**导入导出功能调试问题**

1.序列化问题，序列化宏有两种，一种是只序列化结构成员，一种还会序列化结构名

3.界面handle的消息回调NOTIFY_PARAM_REF和NOTIFY_PARAM_MOVED

4.二进制文件和文本文件的读取

5.组件addfunc不起作用

6.组件copy路径问题

7.组件回调类型错误检查不出来

时间紧





设计总结：

异常考虑没有系统性

正常流程代码编写完成后，需要检查逻辑完整性，是否需要重构，异常要考虑全、日志、开启的线程是否有停止（否则会进程残留），缓存是否需要加锁







1.exit（0）后进程有残留

原因：程序的线程一直在运行没有退出，导致程序没有退出，检查各个模块的反初始化，确保各个模块的线程都停止



2. 数据库表如果有外键，就删除不了有外键的表







调试方法

巡检仪增加流媒体组件后，在初始化rpc时发现读取配置出错，没有该字段。但是在初始化其他模块时读取配置文件是好的。  一步步把会出错的代码复制到错误之前，一点点缩小范围排查得到，是流媒体组件的其中一个类在构造时的问题



