---
tags:
  - seed
  - project
  - guide
created: 2026-05-30
updated: 2026-05-30
topic: tech
---

```cpp
#include <iostream>
using namespace std;
class Circle 
{
    public:
    void draw() { cout << "绘制圆形" << endl; }
};
 class Rectangle 
{
    public:
    void draw() { cout << "绘制矩形" << endl; }
};
// 客户端直接创建对象
int main() {
    string shapeType;
    cout << "输入形状类型 (circle/rectangle): ";
    cin >> shapeType;
    if (shapeType == "circle") {
        Circle circle;
        circle.draw();
    } 
    else if (shapeType == "rectangle") {
        Rectangle rect;
        rect.draw();
    }
    else {
        cout << "无效类型!" << endl;
    }
    return 0;
}
```

#### **旁白脚本 (0:31-1:30)**
**画面**：代码逐行高亮，红色框标记`if-else`部分  
**旁白**：  
_"这是一个简单的图形绘制程序，用户输入形状类型后直接创建对应对象。  
__虽然功能实现了，但所有创建逻辑都堆积在客户端代码中——  
__就像让顾客自己去车间组装产品，既不专业，也难以维护！"_

---

### **2. 问题分析（重点讲解）**
#### **核心问题**
1. **违反开闭原则**：新增形状类型需修改客户端代码
2. **职责不单一**：客户端同时承担对象创建和使用职责
3. **重复代码**：创建逻辑分散在多处

#### **旁白脚本 (1:31-3:00)**
**画面**：代码被红叉覆盖，出现「新增三角形需修改多处」的动画提示  
**旁白**：  
_"想象一下，如果我们想新增一个三角形：  
__① 要修改所有_`_if-else_`_判断  
__② 需要重新编译整个客户端代码  
__③ 可能意外影响已有功能  
__这种设计就像在雷区里跳舞——每次改动都充满风险！"_

---

### **3. 使用简单工厂模式重构**
#### **重构后代码**
cpp

复制

```cpp
// 抽象产品接口
class Shape {
public:
virtual void draw() = 0;
virtual ~Shape() {} // 虚析构确保正确释放资源
};

// 具体产品
class Circle : public Shape {
public:
void draw() override { cout << "绘制圆形" << endl; }
};

class Rectangle : public Shape {
public:
void draw() override { cout << "绘制矩形" << endl; }
};

// 简单工厂
class ShapeFactory {
public:
static Shape* createShape(const string& type) {
    if (type == "circle")    return new Circle();
    if (type == "rectangle") return new Rectangle();
    return nullptr;
}
};

// 客户端代码
int main() {
    string type;
    cout << "输入形状类型 (circle/rectangle): ";
    cin >> type;

    Shape* shape = ShapeFactory::createShape(type);
    if (shape) {
        shape->draw();
        delete shape;
    } else {
        cout << "无效类型!" << endl;
    }
    return 0;
}
```

#### **旁白脚本 (3:01-4:30)**
**画面**：工厂类动态分离创建逻辑，绿色箭头连接客户端与工厂  
**旁白**：  
_"现在我们将创建逻辑抽离到_`_ShapeFactory_`_中：  
__① 客户端只需调用_`_createShape_`_，无需关心对象如何创建  
__② 新增形状类型时，只需扩展工厂类，不影响客户端  
__就像设立了专门的生产部门——各司其职，井然有序！"_

---

### **4. 总结与应用场景**
#### **总结旁白 (4:31-5:00)**
**画面**：思维导图显示定义和场景  
**旁白**：  
**定义**：简单工厂模式通过一个工厂类封装对象的创建过程，将客户端与具体类解耦。  
**应用场景**：

+ 对象创建逻辑相对简单
+ 需要集中管理创建逻辑（如配置化对象生成）
+ 系统需要支持扩展但不需要高度灵活性

**画面**：展示实际项目截图（如UI控件工厂、数据库连接池）  
**旁白**：  
_"在实际项目中，像日志记录器、数据库驱动加载等场景都适用简单工厂模式。"_

---

### **5. 课后练习题**
#### **练习场景：日志记录器**
cpp

复制

```cpp
// 重构前代码（直接在客户端选择日志方式）
class FileLogger {
public:
void log(const string& msg) { /* 写入文件 */ }
};

class ConsoleLogger {
public:
void log(const string& msg) { /* 输出到控制台 */ }
};

// 任务：用简单工厂模式重构此代码
```

#### **旁白脚本 (结尾)**
**画面**：出现代码填空题和倒计时动画  
**旁白**：  
_"现在轮到你了！请为这个日志系统设计一个_`_LoggerFactory_`_，  
__支持根据配置创建文件日志器或控制台日志器。  
__暂停视频思考一下，答案将在下期揭晓！"_

__

__

__

__

__

您确实指出了关键问题！这正是传统工厂模式与现代C++封装思维的 **核心博弈点**。让我们通过 **三级优化方案** 彻底解决这个依赖问题，实现真正的 **全抽象封装**。

---

### _问题定位：客户端依赖具体产品类_
```cpp
// 客户端代码仍需要知道Benz的存在
CarFactory benzFactory = []{ return new Benz(); };  // ❌ Benz暴露了
```

---

### _优化方案1：类型注册机制（编译时解耦）_
#### _实现代码_
```cpp
#include <map>
#include <string>
#include <functional>
#include <memory>

class Car { /* 同前 */ };

// 产品注册表（关键基础设施）
class CarRegistry {
private:
    static std::map<std::string, std::function<Car*()>>& getMap() {
        static std::map<std::string, std::function<Car*()>> instances;
        return instances;
    }

public:
    static void registerCar(const std::string& name, auto&& func) {
        getMap()[name] = std::forward<decltype(func)>(func);
    }

    static Car* create(const std::string& name) {
        if(auto it = getMap().find(name); it != getMap().end())
            return it->second();
        throw std::runtime_error("未知车型");
    }
};

// 自动注册模板（每个车型只需写一次）
template<typename T>
struct AutoRegister {
    explicit AutoRegister(const std::string& name) {
        CarRegistry::registerCar(name, []{ return new T(); });
    }
};

// 具体车型定义（与客户端完全隔离）
namespace {
    class Benz : public Car {
    public:
        void drive() override { std::cout << "奔驰启动\n"; }
        static AutoRegister<Benz> registrar;  // 自动注册
    };
    AutoRegister<Benz> Benz::registrar("benz");

    class BMW : public Car {
    public:
        void drive() override { std::cout << "宝马启动\n"; }
        static AutoRegister<BMW> registrar;
    };
    AutoRegister<BMW> BMW::registrar("bmw");
}

// 客户端代码（零产品类依赖）
int main() {
    std::unique_ptr<Car> car(CarRegistry::create("benz"));
    car->drive();  // 输出：奔驰启动
}
```

#### _关键创新点_
1. **注册机制**_：通过_`_CarRegistry_`_集中管理车型创建逻辑  _
2. **自动注册**_：利用模板在全局初始化阶段完成注册  _
3. **匿名命名空间**_：彻底隐藏具体产品类的存在_

---

### _优化方案2：静态接口+Concept（C++20 终极方案）_
```cpp
#include <concepts>

// 定义Car概念（编译时检查）
template<typename T>
concept Car = requires(T t) {
    { t.drive() } -> std::same_as<void>;
};

// 通用工厂模板
template<Car T>
struct CarFactory {
    static T* create() { return new T(); }
};

// 客户端代码（仅依赖概念）
template<Car T>
void clientCode() {
    T* car = CarFactory<T>::create();
    car->drive();
    delete car;
}

// 具体车型定义（可放在独立模块）
class Benz {
public:
    void drive() { std::cout << "奔驰启动\n"; }
};

// 使用
int main() {
    clientCode<Benz>();  // ✅ 合法，Benz满足Car概念
    // clientCode<int>(); // ❌ 编译错误：不满足Car概念
}
```

#### _优势分析_
+ **零运行时开销**_：全部在编译期解析  _
+ **强类型约束**_：通过Concept确保类型安全  _
+ **极致解耦**_：客户端只需知道概念，无需头文件依赖_

---

### _优化方案3：依赖注入容器（工业级解决方案）_
```cpp
#include <boost/di.hpp>
namespace di = boost::di;

// 定义抽象接口
class ICar {
public:
    virtual void drive() = 0;
    virtual ~ICar() = default;
};

// 具体实现（与客户端隔离）
class Benz : public ICar { /*...*/ };
class BMW : public ICar { /*...*/ };

// 配置注入规则（通常在独立模块）
auto carModule = [] {
    return di::make_injector(
        di::bind<ICar>().to<Benz>()  // 配置默认车型
    );
};

// 客户端代码（仅依赖接口）
class CarUser {
public:
    explicit CarUser(std::shared_ptr<ICar> car) : car_(car) {}
    void use() { car_->drive(); }
private:
    std::shared_ptr<ICar> car_;
};

int main() {
    auto injector = carModule();
    auto user = injector.create<CarUser>();
    user.use();  // 输出：奔驰启动
}
```

#### _生产环境优势_
+ **配置与代码分离**_：通过DI容器管理依赖  _
+ **轻松切换实现**_：修改注入配置即可换车型  _
+ **支持复杂生命周期**_：单例/多例自由控制_

---

### _终极解耦层次对比_
| **方案** | _解耦程度_ | _适用场景_ | _学习成本_ |
| --- | --- | --- | --- |
| _类型注册_ | _运行时解耦，客户端仅依赖字符串key_ | _插件式架构_ | _中等_ |
| _静态接口+Concept_ | _编译时解耦，类型安全极致_ | _高性能核心模块_ | _高（需C++20）_ |
| _DI容器_ | _全系统级解耦，工业级强度_ | _大型应用/微服务架构_ | _高_ |


---

### _决策树：如何选择解耦方案？_
```latex
是否需要运行时动态创建？
├── 是 → 选择类型注册方案
└── 否 → 
    是否需要极致性能？
    ├── 是 → 静态接口+Concept
    └── 否 → DI容器方案
```

_通过这三级进化，我们最终实现了：  
_🔥_ _**客户端代码对具体产品类的零依赖**_  
_🚀_ _**在不损失性能的前提下获得最大灵活性**_  
_💡_ _**让设计模式真正服务于工程实践而非教条**

