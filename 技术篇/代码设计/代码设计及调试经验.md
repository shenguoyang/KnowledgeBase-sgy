1.传输大文件，用内存块+json

2.选择一个序列中的某几项可以用位表示，如用户自定义要导出的文件，每个文件用一位表示

3.避免缓存问题总结，很多情况下通过缓存可以解决很多问题，但也会带来很多问题

   之所以用缓存是因为有多个模块或者不同线程间需要用到该值，

4.硬件连接代码有两种写法，同步和异步，优缺点比较





5.对业务的关键节点处需要拎出来。通过建一个个函数

    需要对一个有多个步骤组成的业务，在每个步骤节点都要有个唯一的入口函数，方便后续功能扩展时，需要对任何一个步骤处加逻辑。

例如，漆面需要增加数据平台功能，需要在一辆车检测完时，发送检测数据给数据平台。如果有个函数表示车辆刚刚检测完就会进入，则只需要在该函数入口处加发送数据即可

任务：重构机器人时序



规范：

私有函数用下划线开头





1.车型copy，删除原图，系统配置导入导出这类问题可否抽象一个框架出来

1.1客户单发起一个任务，可附带各种参数

1.2服务端处理一段自己的任务，再开启一个线程，向各个sensor发送一个任务

1.3 每个sensor也需要开启一个异步任务

1.4 服务单等待各个sensor的任务结束，异常，再回调给客户端





日志：

代码中加日志规范

1.函数入口处加

2.在第三方接口如算法调用处、组件调用处、落盘处、

3.状态变化处，如sessionid，





第三方接口中加耗时函数





每个类中的线程等资源要管理好，容易出错，析构中关线程容易出问题

过长的类型需要重新定义

对象如果在释放的时候会有很多步骤，建议对象new出来，然后delete掉，这样可免去很多代码



在设计时，如果有车辆测量和补偿流程可能同时存在，则需要考虑数据模块需要两份拷贝，不能用单例

